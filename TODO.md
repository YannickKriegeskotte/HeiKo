Datenbank Seite:
Wenn ein Suchfilter eingegeben ist, ein Key geändert wurde und gespeichern gedrückt wurde, wird der neue key nicht angezeigt angezeit, wenn er den Suchfilter enthält.
Wird allerdings under gleichen Bedingungen die Value geändert, wird diese angezeigt.


exe datei machen:
npm install -g pkg
pkg . --targets node18-win-x64 --output heiko.exe